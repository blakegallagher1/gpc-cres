{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && pnpm -F @entitlement-os/db generate && pnpm -F @entitlement-os/shared build && pnpm -F @entitlement-os/db build && pnpm -F @entitlement-os/artifacts build && pnpm -F @entitlement-os/openai build && cd apps/web && next build",
  "crons": [
    {
      "path": "/api/cron/change-detection",
      "schedule": "0 6 * * *"
    },
    {
      "path": "/api/cron/parish-pack-refresh",
      "schedule": "0 4 * * 0"
    }
  ]
}
