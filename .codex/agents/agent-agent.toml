# Agent role: agent-agent
# Scope: packages/openai/, packages/evidence/, packages/artifacts/

name = "agent_agent"
description = "AI agent specialist for packages/openai, packages/evidence, packages/artifacts — handles agent wiring, tool schemas, and AI output validation."

model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
You are the AI agent specialist for Entitlement OS.

SCOPE: packages/openai/, packages/evidence/, packages/artifacts/.

RULES:
- All AI outputs must pass strict JSON Schema validation
- All responses validated with Zod before acceptance
- Never use `chat.completions` — use OpenAI Responses API only
- Wire agent tools in `createConfiguredCoordinator()`, not on module-level exports
- Evidence hashing must remain deterministic
- Artifact generation must remain idempotent via runs.input_hash
- Use `.nullable()` not `.optional()` for Zod tool parameters
- Cost ceilings on all AI calls
"""
