{
  "name": "gpc-admin-dashboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev ./public",
    "deploy": "wrangler pages deploy ./public"
  },
  "devDependencies": {
    "wrangler": "^4.76.0"
  }
}
